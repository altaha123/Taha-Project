fastapi==0.115.*
uvicorn==0.30.*
yfinance>=0.2.61
curl_cffi
pandas
numpy
pyotp
websockets>=12.0
# Used directly by alerts.py, announcements.py, dhan_source.py and
# news_feed.py. It arrived transitively via yfinance until now; pinning it
# explicitly means a yfinance upgrade cannot remove it.
requests>=2.31

# Tests only — the CI workflow installs these separately, so they are listed
# here for anyone running the suite locally rather than pinned into the
# deployed image. httpx is not imported by any test directly; it is what
# fastapi.testclient needs, which is why its absence read as an admin-auth
# failure rather than a missing package.
# pytest>=8.0
# httpx>=0.27
Pillow>=10.0

# Reading the PDF a filing actually is. Almost nothing worth knowing is in the
# exchange headline — the order value, the customer, what management said on
# the call are all in the attachment. Pure Python, no build step, so it cannot
# break a deploy on a free instance for want of a compiler.
pypdf>=4.0

# The optional written summary of an earnings call. Dormant: concalls.py
# imports it lazily and only when ANTHROPIC_API_KEY is set, so until a key is
# present this costs disk on the image and nothing at runtime. It is pinned
# here rather than left out so that switching the feature on is one environment
# variable, not a deploy.
anthropic>=0.40

# Crash reporting. Inert without SENTRY_DSN in the environment — main.py does
# not import it at all in that case — so this costs disk on the image and
# nothing at runtime until it is switched on. Pure Python, no build step.
sentry-sdk>=2.0

# Reading an asset manager's monthly portfolio disclosure, which SEBI requires
# every AMC to publish as a workbook. fund_workbook.py streams these in
# read-only mode because the packs run to fifteen megabytes.
#
# This was missing for a release. The module imported it, the parser was tested
# against a real pack locally, and the deployed instance had no such module —
# so every ingest raised ImportError, got caught as "could not read the
# workbook", and the nightly job reported success while writing nothing. Pure
# Python, no build step.
openpyxl>=3.1

# Both arrive transitively today — anyio under fastapi/starlette, multitasking
# under yfinance — and both are imported directly by this codebase, so an
# upgrade that dropped either would break a deploy for a reason nothing in the
# diff would explain. Pinned for the same reason requests is, above.
anyio>=4.0
multitasking>=0.0.11
